# Claude Code Configuration

Configuración de Claude Code para el proyecto Pengi Med SaaS.

## 📚 Development Documentation

**Todas las guías de desarrollo están en `/docs`**

- **Backend**: [`docs/backend/`](../docs/backend/README.md)
  - Architecture reference
  - Adding new features
  - Database migrations
  - Error handling & responses
  - Permissions system

- **Main docs**: [`docs/README.md`](../docs/README.md)

## ⚙️ Settings

Claude Code settings live in `settings.local.json`. This file is not committed to the repo — it's local only.

## 🔗 For Other IDEs

If you use Cursor, Copilot, VS Code, etc., you don't need to configure anything. Just read `/docs` directly.

All documentation is IDE-agnostic and lives in `/docs/`, so everyone uses the same source of truth.
